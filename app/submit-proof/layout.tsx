/**
 * Submit Proof Layout
 */

import { AppLayout } from "@/components/AppLayout";

export default function SubmitProofLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <AppLayout>{children}</AppLayout>;
}

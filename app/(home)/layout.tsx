/**
 * Home Layout
 * 
 * Simple layout for home page without AppLayout components
 */

export default function HomeLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}

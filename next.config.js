/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  
  // 실험적 기능 (필요시 활성화)
  // experimental: {
  //   typedRoutes: true,
  // },
};

module.exports = nextConfig;

